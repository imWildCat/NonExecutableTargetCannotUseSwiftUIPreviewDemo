# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def shared_dependencies!
  pod 'Kingfisher'
end

target 'NonExecutableTargetCannotUseSwiftUIPreviewDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NonExecutableTargetCannotUseSwiftUIPreviewDemo
  shared_dependencies!
end

target 'SharedUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SharedUI
  shared_dependencies!
end
